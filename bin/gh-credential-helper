#!/bin/bash
# Git credential helper wrapper that ensures gh is found
# This sets PATH before calling gh auth git-credential
# so it works even in contexts without full PATH (e.g., nix-darwin activation)
export PATH="/etc/profiles/per-user/$USER/bin:$PATH"
exec gh auth git-credential "$@"
